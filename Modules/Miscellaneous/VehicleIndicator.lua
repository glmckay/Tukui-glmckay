local T, C, L = unpack(Tukui)

-- local VehicleIndicator = T["Miscellaneous"]["VehicleIndicator"]
-- local Movers = T["Movers"]

-- local function PositionIndicator(self)
--     local Indicator = VehicleSeatIndicator

--     Indicator:SetPoint("BOTTOMLEFT", T.ScreenWidth / 7, T.ScreenHeight / 4)

--     Movers:RegisterFrame(Indicator)
-- end

-- hooksecurefunc(VehicleIndicator, "Enable", PositionIndicator)